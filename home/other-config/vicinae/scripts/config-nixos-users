#!/usr/bin/env bash
# @vicinae.schemaVersion 1
# @vicinae.title config-nixos-users
# @vicinae.mode silent

# kitty --detach hx -w ~/NixOS ~/NixOS/modules/users.nix
zellij run --cwd ~/NixOS -- hx ~/NixOS/modules/users.nix
