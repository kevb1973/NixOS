To Update this App
==================

Get the hash for the new version.
> nix-prefetch-git --url https://github.com/leo-arch/clifm --rev "v1.13"

Edit the default.nix to add the new version number and hash.

Rebuild system.
